// dependencies
const inquirer = require("inquirer")
const fs = require("fs")
const classes = require("./lib/class") 
// blank array of team members to use with building HTML
let teamArray = [];
//blank html string to be added to later
let usefulHTML = "";
//inquirer prompt definition
let employeePrompt = inquirer.prompt([

]).then(function(answer){

})

//html generation loop
let htmlGen;
//write html to file
let writeHTML;